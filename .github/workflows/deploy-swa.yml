name: Deploy to Azure Static Web App

on:
  push:
    branches: [main]
  pull_request:
    types: [opened, synchronize, reopened, closed]
    branches: [main]
  workflow_dispatch:

concurrency:
  group: swa-${{ github.event_name == 'pull_request' && github.head_ref || 'production' }}
  cancel-in-progress: true

permissions:
  id-token: write
  contents: read

jobs:
  deploy-infra:
    runs-on: ubuntu-latest
    name: Deploy Infrastructure
    outputs:
      deployment_token: ${{ steps.infra-token.outputs.deployment_token }}
    steps:
      - uses: actions/checkout@v4

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}

      - name: Deploy Bicep
        uses: azure/arm-deploy@v2
        id: deploy
        with:
          scope: resourcegroup
          resourceGroupName: ${{ vars.AZURE_RESOURCE_GROUP }}
          template: infra/main.bicep
          parameters: >-
            staticWebAppName=${{ vars.SWA_NAME }}
            location=${{ vars.AZURE_LOCATION }}
            sku=${{ vars.SWA_SKU }}
            repositoryUrl=https://github.com/${{ github.repository }}
            repositoryBranch=main

      - name: Output SWA hostname
        run: echo "Static Web App URL - https://${{ steps.deploy.outputs.defaultHostname }}"

      - name: Export SWA deployment token
        id: infra-token
        shell: bash
        run: |
          echo "deployment_token=${{ steps.deploy.outputs.deploymentToken }}" >> "$GITHUB_OUTPUT"

  build-and-deploy:
    needs: deploy-infra
    if: github.event_name != 'pull_request' || github.event.action != 'closed'
    runs-on: ubuntu-latest
    name: Build and Deploy
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - run: npm ci
      - run: npm run build

      - name: Deploy to Azure Static Web App
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ needs.deploy-infra.outputs.deployment_token }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: upload
          skip_app_build: true
          app_location: /
          output_location: dist

  close-pr:
    needs: deploy-infra
    if: github.event_name == 'pull_request' && github.event.action == 'closed'
    runs-on: ubuntu-latest
    name: Close PR staging environment
    steps:
      - name: Close staging environment
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ needs.deploy-infra.outputs.deployment_token }}
          action: close
          app_location: /
